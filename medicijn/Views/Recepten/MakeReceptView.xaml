<?xml version="1.0" encoding="UTF-8"?>
<ContentView
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="medicijn.Views.Recepten.MakeReceptView">
    <ContentView.Content>
        <StackLayout>
            <Label Text="Hey ik ben hier" TextColor="White"/>
            <StackLayout Orientation="Horizontal">
                <Label
                    FontAttributes="Bold"
                    Text="Nieuwe Recept" />
                <Button
                    Padding="0"
                    HorizontalOptions="EndAndExpand"
                    Command="{Binding NewReceptLinePressedCommand}"
                    Text="Add Regel" />
            </StackLayout>
            <ScrollView>
                 <StackLayout BindableLayout.ItemsSource="{Binding Recept.ReceptRegels}">
                    <BindableLayout.ItemTemplate>
                        <DataTemplate>
                            <StackLayout Padding="0, 10">
                                <Label Text="{Binding Medicijn.Naam}" />

                                <StackLayout>

                                </StackLayout>
                            </StackLayout>
                        </DataTemplate>
                    </BindableLayout.ItemTemplate>
                </StackLayout>
            </ScrollView>
        </StackLayout>
    </ContentView.Content>
</ContentView>
