<?xml version="1.0" encoding="UTF-8"?>
<ContentView
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="medicijn.Components.SelectBoxGroup"
    x:Name="this">
    <ContentView.Content>
        <StackLayout BindableLayout.ItemsSource="{Binding Options, Source={Reference this}}">
            <BindableLayout.ItemTemplate>
                <DataTemplate>
                    <StackLayout>
                        <Label Text="{Binding Option}">
                            <Label.Triggers>
                                <DataTrigger TargetType="Label" Binding="{Binding SelectedId, Source={Reference this}}" Value="{Binding Id}">
                                    <Setter Property="BackgroundColor" Value="Red" />
                                </DataTrigger>
                            </Label.Triggers>
                        </Label>
                    </StackLayout>
                </DataTemplate>
            </BindableLayout.ItemTemplate>
        </StackLayout>
    </ContentView.Content>
</ContentView>
